from tkinter import *
import mysql.connector 

def currency_page(root, db, mycur):
    global search
    search = Toplevel(root)
    search.geometry('300x500')
    search.title('Select a currency') 
    pass
